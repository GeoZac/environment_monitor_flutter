/// Enum representing the types of sensor locations.
enum SensorLocationType {
  /// Indicates that the sensor is located indoors.
  indoor,

  /// Indicates that the sensor is located outdoors.
  outdoor,
}
