class AppConstants {
  /// Key for the HTTP Content-Type header.
  static const String contentTypeKey = "Content-Type";

  /// Value for the HTTP Content-Type header when sending JSON data.
  static const String contentTypeValue = "application/json";

  /// Key for the HTTP Authorization header.
  static const String authorizationKey = "Authorization";

  /// Prefix for bearer tokens used in the Authorization header.
  static const String bearerPrefix = "Bearer ";
}
